rules_version = '2';

// DEVELOPMENT-ONLY rules. Locked down in Prompt 17 before any real
// medical data goes near a device.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
